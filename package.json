{
  "name": "gliner-email",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GLiNER 2.5 (194M DeBERTa-v3 encoder, CPU) triaging a real inbox",
  "scripts": {
    "model": "systemd-run --user --scope -q -p MemoryMax=8G -p MemorySwapMax=0 ./.venv/bin/python server/gliner_server.py --port 8750",
    "dev": "bun server/static.js",
    "start": "bun server/static.js"
  },
  "dependencies": { "js-yaml": "^4.1.0" }
}
