{
  "name": "laya-mars",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Laya (421M ModernBERT-large decision model) playing vibe-arcade's MARS RAID",
  "scripts": {
    "game": "bash scripts/fetch-game.sh",
    "model": "systemd-run --user --scope -q -p MemoryMax=12G -p MemorySwapMax=0 ./.venv/bin/python server/laya_server.py --port 8790 --device cuda --threads 8",
    "dev": "bun server/static.js",
    "start": "bun server/static.js",
    "model:cpu": "systemd-run --user --scope -q -p MemoryMax=12G -p MemorySwapMax=0 ./.venv/bin/python server/laya_server.py --port 8790 --device cpu --threads 24"
  }
}