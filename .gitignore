# python env (7.4 GB, rebuildable — see REPRO.md)
.venv/

# pristine HF snapshot (8.5 GB checkpoint) — re-fetch with:
#   huggingface-cli download AlexWortega/openjev --local-dir ./openjev_hf
openjev_hf/

# rendered gameplay videos (32 MB) — regenerated by the doom.py commands in REPRO.md
*.mp4

__pycache__/
*.pyc
_vizdoom.ini

node_modules/
bun.lock

# fetched at setup (see README): vibe-arcade's game, patched in place
web/game/mars.html

# never commit mail: point EMAIL_DB at a corpus outside the repo
data/
*.mbox

# personal inbox taxonomy (real folder names + hypotheses) — see config.yaml.example
config.yaml
